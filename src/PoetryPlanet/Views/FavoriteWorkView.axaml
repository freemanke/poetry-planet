<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:PoetryPlanet.ViewModels"
             xmlns:controls="clr-namespace:PoetryPlanet.Controls"
             mc:Ignorable="d" d:DesignWidth="400" d:DesignHeight="500"
             x:DataType="viewModels:FavoriteWorkViewModel"
             x:Class="PoetryPlanet.Views.FavoriteWorkView">
    <Design.DataContext>
        <viewModels:FavoriteWorkViewModel/>
    </Design.DataContext>
    <StackPanel>
        <TextBlock 
            Text="{Binding Title}" 
            FontSize="24" FontWeight="Medium" TextAlignment="Center" 
            HorizontalAlignment="Center"  Padding="10"
            VerticalAlignment="Center"/>
        <TextBlock 
            Text="{Binding Author}"
            TextWrapping="Wrap" FontSize="20" FontWeight="Light" TextAlignment="Center"
            HorizontalAlignment="Center" 
            VerticalAlignment="Center"
            Margin="10"/>
        <TextBlock 
            Text="{Binding Content}" 
            FontSize="18" FontWeight="Light" TextWrapping="Wrap" TextAlignment="Center"
            HorizontalAlignment="Center" 
            VerticalAlignment="Center"
            Margin="10"/>
        <TabControl>
            <TabItem Header="简介" Foreground="Chocolate">
                <TextBlock 
                    Text="{Binding Intro}" 
                    FontSize="16" TextWrapping="Wrap" 
                    Margin="10"/>
            </TabItem>
            <TabItem Header="译文" Foreground="Chocolate">
                <TextBlock 
                    Text="{Binding Translation}" 
                    FontSize="16" TextWrapping="Wrap" 
                    Margin="10"/>
            </TabItem>
        </TabControl>
        <controls:HorizontalSeparator />
    </StackPanel>
</UserControl>
